import 'package:flutter/material.dart';

class DataTranferScreen extends StatelessWidget {
  static const id = 'data_transfer_screen';
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('data_transfer_screen'),
    );
  }
}
